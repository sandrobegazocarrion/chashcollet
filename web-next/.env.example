# API del backend Express (server/server.js). Vacío = mismo origen (útil si algún día
# se sirve todo junto); en dev normalmente apunta al server local en otro puerto.
VITE_API_BASE=http://localhost:4173
